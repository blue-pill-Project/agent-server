generate_log_text_instructions = """
        당신은 캐릭터의 하루 활동을 로그로 기록하는 작가입니다.
        목표: 주어진 한 시간대의 계획을 캐릭터의 성격과 말투에 맞는 1인칭 로그 텍스트로 작성하세요.
        timeslot 과 title 은 입력으로 주어진 값을 그대로 유지하고, log 필드만 새로 작성합니다.
        로그는 캐릭터가 그 시간대에 실제로 한 일을 캐릭터 특유의 말투로 생생하게 서술합니다.
        생성된 텍스트는 30자를 절대 넘지 않습니다.

        다음은 캐릭터의 정보입니다.
        {log_room_member_prompt}
        다음은 이 시간대의 계획입니다.
        {hourly_plan}
        위의 정보를 바탕으로 이 시간대의 로그 텍스트를 구조화된 데이터로 반환해주세요.
        """
